<div class="card" style="margin-bottom: 20px;">
  
  <div class="card-body">
    <h5 class="card-title"><%= comment.body %></h5>
    <p class="card-text">
      <%= link_to friendship_path(comment.user) do %>
        <%= comment.user.name %></p>
      <% end %>
    <p class="card-text"><small><%= time_ago_in_words(comment.created_at) %></small></p>

    <span id="comment_<%= comment.id %>_likes">
      <%= render "likes/likes", likeable: comment %>
    </span>

    <% if current_user == comment.user || current_user == comment.post.user %>
    	<span> | <%= link_to 'Delete',[comment.post, comment], method: :delete, data: {confirm: 'Are you sure?'} %></span>
    <% end %>
  </div>
  
  <div class="card-footer">
    <h5>Replies(<%= comment.replies.count %>):</h5>
    <hr>
    <div id="replyContainer_<%= comment.id %>">
      <%= render partial: 'comments/showreply', locals: {comment: comment} %>
    </div>
    <div id="replyForm">
      <%= render partial: 'replies/form', locals: {comment: comment}  %>
    </div>

  </div>


  
</div>


